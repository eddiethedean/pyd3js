# Local development environment for pyd3js-array
#
# Keep secrets out of this file. Prefer `.env.local` for machine-specific values.

# Oracle test harness caching (see `tests/oracle/client.py`)
ORACLE_CACHE=1

