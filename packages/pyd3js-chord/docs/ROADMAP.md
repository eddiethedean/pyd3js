# Roadmap

`pyd3js-chord` targets upstream `d3-chord` parity for the pinned version in `upstream_lock.json`.

Quality bar:

- Export parity is auditable (`docs/UPSTREAM_API.md` + README compatibility matrix)
- Python unit tests + oracle parity where feasible
- Vendored upstream JS test-suite gate (`pytest -m upstream`)
- 100% Python line coverage by default

