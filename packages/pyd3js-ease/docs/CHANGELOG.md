# Changelog

## 0.1.0

- Initial release: full port of **`d3-ease@3.0.1`** exports (easing functions, polynomial/back/elastic configurators).
- Pytest coverage aligned with upstream Mocha numeric expectations.
- Documentation and QA aligned with `pyd3js-array`: `docs/USER_GUIDE.md` with verified examples, README compatibility matrix and badges, `test_ease_docs_examples` / `test_ease_parity_matrix`, package `.gitignore`, normalized `docs/UPSTREAM_API.md`, and `Documentation` project URL in `pyproject.toml`.
- README notes zero third-party runtime dependencies for PyPI installs.
