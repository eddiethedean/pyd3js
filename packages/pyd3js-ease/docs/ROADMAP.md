# pyd3js-ease roadmap

Python port of [`d3-ease`](https://github.com/d3/d3-ease) pinned via root `upstream_lock.json`.

## Near term

- Optional vendored Mocha gate (`pytest -m upstream`) mirroring `pyd3js-array`, if the upstream repo is checked in under `upstream/d3-ease`.

## Longer term

- Oracle-style numeric checks against Node for representative samples (optional; unit tests already match upstream Mocha tables).
