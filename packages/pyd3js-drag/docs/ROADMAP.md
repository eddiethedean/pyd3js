# Roadmap

## Near-term

- Port remaining upstream behavioral tests (beyond export checks) as needed by downstream packages.
- Tighten DOM / event emulation to support realistic pointer/touch flows.

