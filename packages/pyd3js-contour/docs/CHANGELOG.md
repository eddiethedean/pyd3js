# Changelog

All notable changes to `pyd3js-contour` will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## Unreleased

## 0.1.0

### Added

- Initial release: Python port of **`d3-contour@4.0.2`** public API (`contours`, `contourDensity`).
- `docs/UPSTREAM_API.md` compatibility matrix and oracle-tagged parity coverage.
- Optional vendored upstream JS gate (`pytest -m upstream`) under `upstream/d3-contour`.
