# Roadmap

## Near term

- Optional NumPy typed-array interoperability for `interpolateNumberArray` / `isNumberArray`.
- Richer SVG `transform` parsing beyond `matrix(...)` for `interpolateTransformSvg` if needed.

## Longer term

- Oracle-style checks against Node for representative `interpolate` dispatch cases.
