# Roadmap

## Near-term

- Fill any remaining gaps in upstream test ports and tighten parity for edge-case DOM/event behavior.
- Add oracle-style parity tests where JSON-safe and useful.

## Longer-term

- Expand DOM support (selectors, serialization, event model) as needed by downstream modules (`d3-transition`, `d3-zoom`).

